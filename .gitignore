.env
__pycache__/
.venv/
*.pyc
.DS_Store
chroma_db/

# Generated/downloaded data — not tracked
data/raw/
data/interim/cleaned_tables/
data/indexes/
data/*.csv
data/*.parquet

# Extracted text corpus and chunks ARE tracked (needed for deployment)
# data/interim/extracted_text/ — tracked
# data/processed/chunks.jsonl  — tracked

# Partial eval results (mid-run saves, replaced on completion)
eval/results/*.partial.json

# Claude Code session memory — not part of the project
memory/
